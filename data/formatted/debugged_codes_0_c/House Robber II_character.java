The bug in the provided code is a missing semicolon on line 8:
```java
int[] t = new int[nums.length]
```
To fix this bug, we simply need to add the missing semicolon:
```java
int[] t = new int[nums.length];
```
Other than this bug, the code appears to be correct.